Also it's possible to define rules for ssh in ~/.ssh/config, e.g. based on aliases:

   Host github
      HostName github.com 
      User git
      IdentityFile "~/.ssh/id_rsa_git"

   Host git
      HostName github.com 
      User git
      IdentityFile "~/.ssh/some_other_git"
